# Legacy Functions Host Config

This folder stores Azure Functions host/local settings files that are no longer used by the active runtime.

Active runtime is the Express server at src/api/server.ts.
