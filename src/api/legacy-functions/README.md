# Legacy Azure Functions Backend

This folder contains the previous Azure Functions implementation that was replaced by the Express server in ../server.ts.

Status:
- Retained for reference only.
- Not compiled by src/api/tsconfig.json.
- Not used by active npm scripts.

Active runtime:
- Use npm scripts in src/api/package.json.
- The running backend is the Node server in src/api/server.ts.
