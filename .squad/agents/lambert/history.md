# Lambert History

## Learnings
- Project: pomo-test1
- User: aisling
- Migration risk area: preserve behavior while changing hosting/runtime
- 2026-06-15: Cheapest current executable validation is `npm run smoke:api`, which covers unauthenticated `GET /api/lists` plus optional authenticated list/task CRUD when a server is running and auth test headers are provided.
- 2026-06-15: No automated test currently covers frontend logout redirect state reset, stale auth-cache fallback behavior in `loadAuthState`, or background persistence/load on startup/home-screen launch.
- 2026-06-15: Current logout regression risk is split between immediate local state reset in `redirectToLogout` and later fallback-to-cache branches in `loadAuthState`; the narrowest future validation is a browser-level check that clicks logout, asserts `/.auth/logout?post_logout_redirect_uri=...`, and verifies the signed-in UI/cache clear before redirect.
- 2026-06-15: Current stale signed-in cache risk is that any non-OK `/api/auth/me` response still restores cached auth, so the narrowest validation is a browser test that seeds `pomodoro-auth-cache`, mocks `/api/auth/me` to return `401`, and asserts the app does not remain signed in after logout.
- 2026-06-15: Current saved-to-home-screen background risk is localStorage-only restoration; the narrowest validation is a browser test that seeds `pomodoro-background` and `pomodoro-background-opacity` before app bootstrap and asserts the background overlay/style is applied on first render after reload.
- 2026-06-15: No local Dallas/Parker worktree changes were present during this check, so there was no touched slice to revalidate beyond inspecting current main-branch auth/background behavior.
- 2026-06-16: Verified on 390px viewport that subtasks render in a dedicated section below task-level options and long subtask names wrap without collapsing into an unusable narrow control column.
- 2026-06-16: `npm run smoke:api` still targets `http://localhost:7071`; failure there is expected when API server is not running and should not be treated as a frontend-only regression signal.
- 2026-06-16: Independent QA verification passed all mobile readability acceptance criteria with no code changes required.
- 2026-06-24: Verified `handleSelectTaskList` collapses all tasks on list open by setting `allTasksCollapsed` true and forcing each task `collapsed: true`, preserving the requested auto-collapse behavior.
- 2026-06-24: Verified local-mode/logged-out warning is now dismissible via `isLocalModeNoticeDismissed`; dismissal is session-scoped and resets when local-mode warning no longer applies.
- 2026-06-24: Verified compact timer includes both reduced timer sizing and an explicit muted-state note (`Muted`) when sound is off.
- 2026-06-24: API smoke and API build remain environment-sensitive gates: smoke fails if no local API server runs on `http://localhost:7071`, and direct `npm run build:api` from repo root can fail when API package deps/types are not installed in the active node_modules layout.
- 2026-06-24: App icon integration is currently incomplete in app source: no project-owned icon/manifest assets were found outside dependencies, and `index.html` has no favicon/apple-touch/manifest links.
- 2026-08-05: APPROVED the focused High Priority read-only task-card change. Subtasks render only when present; completed/incomplete state is conveyed visually and to screen readers; no mutation controls remain in the read-only branch; and card click plus Enter/Space still open the source task without nested interactive-element conflicts.
- 2026-08-05: `npm run build` passes for the TaskItem change. Existing editable TaskItem behavior is outside the changed branch and unchanged by the diff; CSS optimizer and bundle-size warnings are unrelated and non-blocking.
- 2026-08-05: PASS local browser smoke at 1440x900 and 390x844 using isolated anonymous localStorage data. High Priority showed one parent with completed and incomplete subtasks, distinct accessible labels and line-through styling, no card overflow/row overlap, and card click opened the expanded source task. Screenshots: `/tmp/pomo-high-priority-desktop-keyboard.png`, `/tmp/pomo-high-priority-mobile-keyboard.png`. Non-blocking: anonymous startup logs the expected 401/list JSON fallback errors, and the local-mode notice visibly truncates at 390px while the High Priority content remains intact.
- 2026-08-05: APPROVED explicit per-subtask iteration labels in the High Priority read-only view. Live isolated-browser fixtures verified `1 iteration` and `3 iterations`, distinct completed/incomplete accessible state and line-through styling, zero nested mutation controls, pointer and Enter source navigation, and no document/card overflow at exact 1440x900 and 390x844 viewports; the long mobile label wrapped to two lines without overlap. `git diff --check -- src/components/TaskItem.tsx` passed. Clear desktop capture: `/tmp/pomo-iteration-counts-desktop.png`; integrated-browser mobile/element screenshot export was clipped by the host and was not used as gate evidence. The Vite server remained running at `http://127.0.0.1:5000/`.
- 2026-08-05: APPROVED the task-loading dependency fix. Replacing `taskLists` identity with `highPriorityListLoadKey` prevents anonymous snapshot object recreation from reloading and re-collapsing an expanded Personal task, while active-list add/remove/archive and ordering changes still reload High Priority. Ordinary anonymous/local and remote list loading remains triggered by selected list, auth mode, or explicit refresh; no stale closure or effect loop was found. `npm run build` and `git diff --check` passed; editor diagnostics reported no errors. Focused ESLint was unavailable because ESLint 9 found no `eslint.config.*`. Residual risk: the reported expand/Add Subtask interaction still requires the user's final manual UX test and was not claimed here.
- 2026-08-05: REJECTED the High Priority per-card subtask minimizer because the read-only card remains `role="button"`/`tabIndex={0}` while containing a native chevron `<button>`. A button-role ancestor cannot contain a separate interactive descendant without invalid/ambiguous semantics for keyboard and assistive-technology users. Required remediation: remove source navigation semantics from the chevron's ancestor and expose source navigation through a separate native button/link (or restructure the card so the source-navigation control and chevron are siblings with no interactive ancestor/descendant relationship); preserve independent Enter/Space activation and propagation handling for both controls.
- 2026-08-05: Static review otherwise passed: the chevron is conditional on `task.subtasks.length > 0`, defaults expanded per mounted card, state is independent per `TaskItem`, click and keydown stop propagation, metadata and subtask count remain outside the conditional list, expanded completion/iteration rows are unchanged, and the editable branch is unchanged by the diff. The dynamic aria-label and `aria-expanded` switch correctly between Hide/Show, but no matching `title` is present if visible hover text is required. `npm run build`, `git diff --check`, and editor diagnostics passed; existing CSS optimizer and bundle-size warnings are unrelated. Residual risk: no fresh browser automation was available in this gate, so mobile clipping and real pointer/keyboard navigation remain for the user's final manual test after remediation.
- 2026-08-05: APPROVED Ripley's independent revision of the High Priority read-only subtask minimizer. The card wrapper is now structurally noninteractive, while native `Open source` and chevron buttons are siblings with no interactive ancestor/descendant relationship. Source navigation is visible, keyboard accessible, and separately labeled; the chevron independently toggles only the subtask list, starts expanded, and exposes synchronized dynamic `aria-label`, `title`, and `aria-expanded` state. Iteration/time/subtask metadata remains visible when collapsed, no mutation controls appear in the read-only branch, and the editable branch is unchanged by the diff. Mobile flex/min-width/wrapping classes are reasonable for the existing 390px baseline. `npm run build`, `git diff --check`, and editor diagnostics for `TaskItem.tsx` and `App.tsx` passed; existing CSS optimizer and bundle-size warnings remain unrelated. Repository-local Playwright is unavailable, so final pointer/keyboard behavior and narrow-mobile clipping remain residual manual-test risk.
- 2026-08-05: APPROVED Ripley's High Priority control-order and reordering revision. Read-only rows now place a real subtask chevron (or noninteractive 28px alignment spacer), separately labeled source-title button, and right-side up/down buttons as non-nested sibling controls. The chevron only changes local subtask visibility; source navigation remains separate; reorder buttons call the existing `onTouchReorder('up'/'down')` fallback, expose labels/titles, and honor composite-key-derived disabled states. `App.tsx` still identifies High Priority tasks by `${listId || 'unknown'}::${id}`, reorders the rendered task array, and persists that composite order through `highPriorityTaskOrder`. Collapsed iteration/time/subtask metadata and expanded completion/iteration labels remain intact, while the editable branch is unchanged. `npm run build`, `git diff --check`, and editor diagnostics passed; `http://127.0.0.1:5001/` returned HTTP 200. Existing CSS optimizer and bundle-size warnings are unrelated. Repository-local Playwright is unavailable, leaving final pointer/keyboard behavior and narrow-mobile overlap as residual manual-test risk.
- 2026-08-05: APPROVED Ripley's read-only visibility initializer change to `false`. High Priority cards with subtasks now start collapsed with a right chevron, `aria-expanded=false`, and matching `Show subtasks for ...` label/title, then switch to a down chevron, `aria-expanded=true`, and matching `Hide subtasks for ...` text when toggled. The state is local to each `TaskItem`, so cards remain independent; editable collapse still uses the separate persisted `task.collapsed`/`toggleCollapse` path. Iteration/time/subtask-count metadata and reorder controls remain outside the conditional subtask list, cards without subtasks retain the 28px alignment spacer, and source-title navigation remains a separate sibling button. `npm run build`, `git diff --check`, editor diagnostics, and HTTP availability at `http://127.0.0.1:5001/` passed. Existing CSS optimizer and bundle-size warnings are unrelated. Repository-local Playwright is unavailable, so pointer/keyboard state transitions and narrow-mobile layout remain residual manual-test risk.
